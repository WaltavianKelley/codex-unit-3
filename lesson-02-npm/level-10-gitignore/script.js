import terminalLink from 'terminal-link';

const link = terminalLink('My Website', 'https://sindresorhus.com');
console.log(link);

console.log("Gitignore is the file that helps you not push sensitive or unwanted data to your repository.")